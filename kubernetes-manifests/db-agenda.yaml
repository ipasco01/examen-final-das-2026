# kubernetes-manifests/db-agenda.yaml
#
# Equipo 5 (Deployment) — issue "Datos y broker dentro del despliegue". Ver el comentario
# completo del patrón en db-auth.yaml: StatefulSet + volumen persistente + contraseña desde el
# Secret app-runtime-secrets (no versionado).
apiVersion: v1
kind: Service
metadata:
  name: db-agenda-service # DNS interno; coincide con DB_HOST en ms-agenda.yaml
spec:
  selector:
    app: db-agenda
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db-agenda
spec:
  serviceName: db-agenda-service
  replicas: 1
  selector:
    matchLabels:
      app: db-agenda
  template:
    metadata:
      labels:
        app: db-agenda
    spec:
      containers:
        - name: postgres
          image: postgres:14-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "user_agenda"
            - name: POSTGRES_DB
              value: "db_agenda"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-runtime-secrets
                  key: AGENDA_DB_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "user_agenda", "-d", "db_agenda"]
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "user_agenda", "-d", "db_agenda"]
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: db-agenda-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: db-agenda-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
