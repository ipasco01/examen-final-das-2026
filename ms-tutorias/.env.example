# ms-tutorias/.env.example
PORT=3000

# Configuración de Logging
LOG_LEVEL=info
SERVICE_NAME=MS_Tutorias

# URLs de los servicios dependientes
MS_USUARIOS_URL=http://localhost:3001/usuarios
MS_AGENDA_URL=http://localhost:3002/agenda
# MS_NOTIFICACIONES_URL: eliminada el 20/07. ms-tutorias no le habla a ms-notificaciones por HTTP,
# lo hace por RabbitMQ (patron outbox). Su unico lector era un cliente que nadie importaba.

# --- RabbitMQ ---
RABBITMQ_URL=amqp://localhost:5672

# --- JWT (debe coincidir con el secreto usado por ms-auth, ms-usuarios y ms-agenda) ---
JWT_SECRET=local-dev-jwt-secret-change-me

# --- Base de Datos ---
DB_HOST=localhost # Usamos localhost para desarrollo local sin Docker
DB_PORT=5434
DB_USER=user_tutorias
DB_PASSWORD=password_tutorias
DB_NAME=db_tutorias
DB_STATEMENT_TIMEOUT_MS=5000
DB_CONNECTION_TIMEOUT_MS=5000
DB_POOL_MAX=15
DB_WORKER_POOL_MAX=3

# --- Timeout global de la Saga (S1) ---
SAGA_TIMEOUT_MS=15000

# --- Reintentos (backoff exponencial + jitter) al llamar a ms-usuarios/ms-agenda ---
# Solo se reintentan fallos de infraestructura (timeout/red); nunca una respuesta HTTP real
# (404/409) ni cuando el Circuit Breaker ya está abierto (ver retry.util.js).
RETRY_USUARIOS_MAX_INTENTOS=3
RETRY_USUARIOS_BASE_DELAY_MS=150
RETRY_AGENDA_MAX_INTENTOS=3
RETRY_AGENDA_BASE_DELAY_MS=150

# --- Compensación síncrona de agenda (rollback de la Saga / cancelación) ---
COMPENSACION_AGENDA_MAX_INTENTOS=3
COMPENSACION_AGENDA_BASE_DELAY_MS=200

# --- Patrón Outbox de notificaciones (D2) ---
OUTBOX_POLL_INTERVAL_MS=3000
OUTBOX_MAX_INTENTOS=5
OUTBOX_BATCH_LIMIT=20

# --- Worker de compensación de agenda pendiente (D6) ---
COMPENSACION_PENDIENTE_POLL_INTERVAL_MS=5000
COMPENSACION_PENDIENTE_MAX_INTENTOS=3
COMPENSACION_PENDIENTE_BATCH_LIMIT=20

# --- Worker de reconciliación de tutorías PENDIENTE huérfanas (S2) ---
RECONCILIACION_PENDIENTE_UMBRAL_MS=300000
RECONCILIACION_POLL_INTERVAL_MS=60000
RECONCILIACION_BATCH_LIMIT=20